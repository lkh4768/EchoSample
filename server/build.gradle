jar {
    manifest {
        attributes(
                'Main-Class': 'xyz.swwarehouse.echoserver.Main'
        )
    }

    /*
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    } with jar
    */
}
